# Exercise 1 / 2: move the port and the JWT secret here.
#
# Add `ktor.deployment.port` with the value "$PORT:8080" (the PORT variable,
# 8080 when it is not set) and `jwt.secret` with the value
# "$JWT_SECRET:change-me". `EngineMain` reads this file; nothing in the
# source knows either value.
ktor:
  application:
    modules:
      - configuration.ApplicationKt.module
