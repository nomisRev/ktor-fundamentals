# Exercise 1 / 3: the host and the port live here, not in main.
#
# Add a `server` section with `host: "$HOST:0.0.0.0"` and `port: "$PORT:8080"`:
# the environment variable when it is set, the value after the colon when it
# is not. Nothing in the source knows either value.
