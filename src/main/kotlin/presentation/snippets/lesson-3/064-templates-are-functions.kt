// Generated by slidev-theme-kotlin from lessons/lesson-3.md, slide 64 "Templates are functions".
// Do not edit: change the slide and run `slidev-kotlin-snippets` again.

package presentation.snippets.lesson3.slide64

import presentation.support.*
import io.ktor.server.html.respondHtml
import io.ktor.server.routing.RoutingContext
import kotlinx.html.*

suspend fun RoutingContext.spell(req: Spell) {
  call.respondHtml {
    body {
      h1 {
        notEmpty(req.name, emptyMessage = "No name") {
          +"${req.name.first().uppercaseChar()}"
          req.name.drop(1).forEach { +" - ${it.uppercaseChar()}" }
        }
      }
    }
  }
}

fun FlowContent.notEmpty(
  value: String,
  emptyMessage: String,
  content: FlowContent.() -> Unit,
) {
  if (value.isEmpty()) +emptyMessage else content()
}
